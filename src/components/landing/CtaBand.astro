---
---

<section class="fr-cta fr-reveal" id="cta">
	<div class="fr-cta-inner">
		<h2>Need a team that can actually own the outcome?</h2>
		<p>No juniors. No handoffs. Talk to the team that actually runs the work.</p>
		<a class="fr-btn fr-btn-primary" href="mailto:sales@kpimedia.sg">See What We Can Do</a>
	</div>
</section>

<style>
	.fr-cta {
		background: var(--fr-canvas);
		padding: var(--fr-xxl) var(--fr-xs);
		text-align: center;
		border-top: 1px solid var(--fr-hairline);
	}
	.fr-cta-inner {
		max-width: 720px;
		margin: 0 auto;
	}
	h2 {
		font-size: 36px;
		font-weight: 500;
		letter-spacing: -0.36px;
		color: var(--fr-ink);
	}
	p {
		font-size: 16px;
		color: var(--fr-body);
		margin: var(--fr-xs) 0 0;
	}
	.fr-cta a {
		margin-top: var(--fr-lg);
	}
</style>
